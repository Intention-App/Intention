query myBoard ($id: String!) {
  myBoard (id: $id) {
    ...RegularBoard
    
    tasklistOrder
    tasklists {
      ...RegularTasklist

      taskOrder
      tasks {
        ...RegularTask
      }
    }
  }
}
