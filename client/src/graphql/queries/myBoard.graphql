query myBoard ($id: String!) {
  myBoard (id: $id) {
    ...RegularBoard
    tasks {
      ...RegularTask
    }
    
    tasklistOrder
    tasklists {
      ...RegularTasklist

      taskOrder
    }
  }
}
