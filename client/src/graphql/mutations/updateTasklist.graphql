mutation updateTasklist($id: String!, $title: String, $color: String, $taskOrder: [String!]) {
  updateTasklist (
    id: $id,
    options:{
        title: $title,
        color: $color,
        taskOrder: $taskOrder
    }
  ) {
    ...RegularTasklist
  }
}