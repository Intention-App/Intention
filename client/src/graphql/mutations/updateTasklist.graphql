mutation updateTasklist($id: String!, $title: String, $color: String) {
  updateTasklist (
    id: $id,
    options:{
        title: $title,
        color: $color,
    }
  ) {
    ...RegularTasklist
  }
}